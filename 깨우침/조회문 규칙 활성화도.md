```mermaid
flowchart TB
    classDef input fill:#eef7ff,stroke:#4a90e2,stroke-width:1.5px,color:#111,font-size:12px;
    classDef rule fill:#eefaf0,stroke:#2e8b57,stroke-width:1.5px,color:#111,font-size:12px;
    classDef out fill:#f6f0ff,stroke:#7a5af8,stroke-width:1.5px,color:#111,font-size:12px;
    classDef final fill:#fff4e8,stroke:#e67e22,stroke-width:2px,color:#111,font-size:12px;

    Q["조회 요구사항"]:::input

    I1{"입력층 ① 출력·원천<br/>무엇을 출력하는가?<br/>어디서 가져오는가?"}:::input
    I2{"입력층 ② 결합·행 조건<br/>여러 테이블인가?<br/>개별 행을 제한하는가?"}:::input
    I3{"입력층 ③ 집계·그룹<br/>집계 함수가 필요한가?<br/>기준별 집계인가?"}:::input
    I4{"입력층 ④ 그룹 조건·정렬<br/>집계 결과를 제한하는가?<br/>출력 순서가 필요한가?"}:::input

    R1["규칙층 ① 기본 골격<br/>테이블 조회는<br/>SELECT·FROM 구성"]:::rule
    R2["규칙층 ② 결합·행 필터<br/>여러 테이블 → JOIN / ON<br/>개별 행 조건 → WHERE"]:::rule
    R3["규칙층 ③ 집계 규칙<br/>집계식 → SELECT<br/>기준별 집계 → GROUP BY<br/>전체 집계는 GROUP BY 불필요"]:::rule
    R4["규칙층 ④ 후처리 규칙<br/>그룹·집계 조건 → HAVING<br/>최종 순서 → ORDER BY"]:::rule

    O1["출력층 ①<br/>SELECT<br/>FROM"]:::out
    O2["출력층 ②<br/>필요 시 JOIN / ON<br/>필요 시 WHERE"]:::out
    O3["출력층 ③<br/>SELECT의 집계식<br/>필요 시 GROUP BY"]:::out
    O4["출력층 ④<br/>필요 시 HAVING<br/>필요 시 ORDER BY"]:::out

    F["최종 SQL 조립<br/>SELECT → FROM ＋ JOIN / ON → WHERE<br/>→ GROUP BY → HAVING → ORDER BY"]:::final

    Q --> I1
    Q --> I2
    Q --> I3
    Q --> I4

    I1 --> R1
    I2 --> R2
    I3 --> R3
    I4 --> R4

    R1 --> O1
    R2 --> O2
    R3 --> O3
    R4 --> O4

    O1 --> F
    O2 --> F
    O3 --> F
    O4 --> F

    linkStyle 0,1,2,3 stroke:#4a90e2,stroke-width:2px;
    linkStyle 4,5,6,7 stroke:#2e8b57,stroke-width:3px;
    linkStyle 8,9,10,11 stroke:#7a5af8,stroke-width:3px;
    linkStyle 12,13,14,15 stroke:#e67e22,stroke-width:3px;