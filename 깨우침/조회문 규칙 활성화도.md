flowchart LR
    classDef input fill:#eef7ff,stroke:#4a90e2,stroke-width:1.5px,color:#111;
    classDef rule fill:#eefaf0,stroke:#2e8b57,stroke-width:1.5px,color:#111;
    classDef out fill:#f6f0ff,stroke:#7a5af8,stroke-width:1.5px,color:#111;
    classDef final fill:#fff4e8,stroke:#e67e22,stroke-width:1.5px,color:#111;
    classDef note fill:#fff8e6,stroke:#c28f2c,stroke-dasharray: 5 3,color:#111;

    Q["조회 요구사항"]:::input

    subgraph INPUT["입력층: 요구사항 피처"]
        direction TB
        I1{"출력할 열·표현식이 필요한가?"}:::input
        I2{"여러 테이블을 함께 조회하는가?"}:::input
        I3{"개별 행 조건이 필요한가?"}:::input
        I4{"집계 함수가 필요한가?"}:::input
        I5{"기준별로 묶어야 하는가?"}:::input
        I6{"집계 결과를 다시 조건으로 거를 것인가?"}:::input
        I7{"결과 정렬이 필요한가?"}:::input
    end

    subgraph RULE["규칙층: 절 활성화 규칙"]
        direction TB
        R0["기본 골격 규칙<br/>조회문이면 SELECT, FROM은 기본"]:::rule
        R1["조인 규칙<br/>여러 테이블이면 FROM 내부에 JOIN / ON 추가"]:::rule
        R2["행 필터 규칙<br/>개별 조건이면 WHERE 추가"]:::rule
        R3["집계 표현 규칙<br/>집계 함수가 필요하면 SELECT에서 집계식 사용"]:::rule
        R4["그룹 규칙<br/>기준별 집계가 필요하면 GROUP BY 추가"]:::rule
        R5["그룹 필터 규칙<br/>집계 결과 조건이면 HAVING 추가"]:::rule
        R6["정렬 규칙<br/>순서가 중요하면 ORDER BY 추가"]:::rule
    end

    subgraph OUTPUT["출력층: SQL 절"]
        direction TB
        O1["SELECT"]:::out
        O2["FROM"]:::out
        O3["JOIN / ON<br/>(FROM 내부 구조)"]:::out
        O4["WHERE"]:::out
        O5["GROUP BY"]:::out
        O6["HAVING"]:::out
        O7["ORDER BY"]:::out
    end

    F["최종 SQL 조립<br/>SELECT → FROM(+JOIN/ON) → WHERE<br/>→ GROUP BY → HAVING → ORDER BY"]:::final

    N1["주의<br/>집계 함수가 필요하다고 해서<br/>항상 GROUP BY가 필요한 것은 아님"]:::note

    Q --> I1
    Q --> I2
    Q --> I3
    Q --> I4
    Q --> I5
    Q --> I6
    Q --> I7

    I1 --> R0
    I2 --> R1
    I3 --> R2
    I4 --> R3
    I5 --> R4
    I6 --> R5
    I7 --> R6

    I4 -.-> N1
    I5 -.-> N1

    R0 --> O1
    R0 --> O2
    R1 --> O3
    R2 --> O4
    R4 --> O5
    R5 --> O6
    R6 --> O7

    O1 --> F
    O2 --> F
    O3 --> F
    O4 --> F
    O5 --> F
    O6 --> F
    O7 --> F

    linkStyle 0,1,2,3,4,5,6 stroke:#4a90e2,stroke-width:2px;
    linkStyle 7,8,9,10,11,12,13 stroke:#2e8b57,stroke-width:3px;
    linkStyle 16,17,18,19,20,21,22 stroke:#7a5af8,stroke-width:3px;
    linkStyle 23,24,25,26,27,28,29 stroke:#e67e22,stroke-width:3px;
    linkStyle 14,15 stroke:#888,stroke-width:2px,stroke-dasharray: 5 3;