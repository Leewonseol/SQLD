# SQLD 1과목 개념 검증 및 수정 보고서

## 1. 검토 범위

**검토한 저장소 경로 (전체 검색 후, 아래 세 경로를 축으로 대조)**
- `SQLD-1과목-핵심법리판례집/`(00~08장 전체: 헌법·결정트리·고난도문제·표본감사보고서·31개 법리 판례 파일)
- `Concepts/`(엔터티·관계·식별자·정규화·NULL 등 Zettelkasten 노트)
- `SQLD-시험직전-압축/`(NULL·3값논리·집계함수 압축 노트)
- 보조 대조: `NULL-집중학습/`(14개 모듈), `깨우침/`, `_원본 자료/`, `Questions/`·`Propositions/`(M01-Q01~Q50)

**검토한 DOCX/PDF/Markdown/생성 스크립트**
- `SQLD_1과목_협업형_본문_5줄지문_문제_수정본 (1).docx` (사용자 업로드본, 이전 세션에서 이어 작업 중이던 동일 파일)
- 위 DOCX는 **저장소에 원본 Markdown이나 생성 스크립트가 존재하지 않는 독립 산출물**이다(저장소 검색 결과 이 DOCX의 본문·문항과 동일한 텍스트를 담은 `.md`/생성 스크립트를 찾지 못했다). 따라서 "생성 스크립트를 먼저 고친다"는 원칙은 이번 DOCX 자체에는 해당하지 않으며, DOCX는 DOCX 원본(`word/document.xml`)을 직접 수정했다.
- 이번에 새로 저장소에 푸시된 `2025-2011학년도 평가원 독서 모음 A4.pdf`는 SQLD와 무관한 수능 국어 자료로, 별도 요청(문장·문항 스타일 참고용)으로 이미 이전 턴에서 처리했으며 이번 보고서의 대상이 아니다.

## 2. GitHub 원본부터 잘못된 개념

| 개념 | 파일 경로 | 기존 문장 | 문제점 | 수정 내용 |
| --- | --- | --- | --- | --- |
| (해당 없음) | — | — | — | — |

이번 검토에서 저장소 원본(`SQLD-1과목-핵심법리판례집`, `Concepts`, `SQLD-시험직전-압축`)에서 사실관계 오류를 찾지 못했다. 아래 4-1~4-5로 지정된 5개 항목을 모두 `00-SQLD-1과목-헌법.md` 및 해당 장의 상세 판례 파일(MOD-01, ENT-01, REL-01, MOD-02, PERF-01)과 원문 대조했고, 5개 중 3개(4-1, 4-2, 4-3)는 DOCX 문장이 이미 저장소 원문과 사실상 동일했으며, 나머지 2개(4-4, 4-5)는 저장소 오류가 아니라 **지난 세션에서 DOCX를 수능 지문 스타일로 다듬는 과정에서 내가 추가한 문장이 저장소 원문보다 과장되거나 저장소가 강조하는 예외를 누락한 경우**였다. 자세한 근거는 3장·5장 참고.

## 3. 문서 생성 과정에서만 발생한 오류

| 개념 | 원본 파일 | 생성 파일 | 변형된 부분 | 수정 내용 |
| --- | --- | --- | --- | --- |
| 4-4. 모델링 단계 순서의 절대성 | `01-데이터모델링-총론/MOD-02-개념논리물리모델.md`(순서를 "뒤바꾸는 함정"만 경고, "생략·반복 불가"는 서술하지 않음) | DOCX 1부 3번째 문단 | 지난 턴에 수능 지문체로 다듬으며 "세 단계는 순서를 건너뛰지 않으며"라는 절대적 문장을 임의로 추가함 — 저장소는 "순서를 뒤바꾸면 오답"이라고만 하지 "현실에서 절대 생략·반복되지 않는다"고 단정하지 않음 | "세 단계는 순서를 건너뛰지 않으며"를 삭제하고 "세 단계는 개념적·논리적·물리적 순서로 진행되며"로 완화 |
| 4-5. 반정규화 4기법과 무결성 위험 | `07-데이터모델과-SQL/PERF-01-반정규화.md`("원칙적으로 무결성 위험을 감수하는 대가로 성능을 얻지만, 예외적으로 '중복 관계 추가'는 무결성 위험 없이 성능을 향상시킬 수 있는 기법") | DOCX 해설26(구 해설22) | 문항22(현 문항26) 해설에서 4개 반정규화 기법(병합·수직분할·중복저장·중복관계추가)을 나열만 하고, 저장소가 강조하는 "이 중 중복 관계 추가만 무결성 위험이 없다"는 예외 조항을 누락함 — 반정규화 기법 자체를 잘못 서술한 것은 아니었음(수직/수평 분할이 반정규화 기법이라는 서술 자체는 정확) | "이 네 기법 중 중복 관계 추가만 데이터 자체를 복제하지 않아 무결성 위험이 없다" 문장을 추가 |

**참고**: 지난 턴에서 이미 수정했던 "문항22(현 문항26): 수직/수평 분할도 반정규화 기법이다"라는 정정 자체는 `PERF-01-반정규화.md`, `00-SQLD-1과목-헌법.md` 제27조, `00-SQLD-1과목-고난도문제.md`(라인 3951, 4179, 4357 등 다수)와 정확히 일치하는 것으로 재확인했다. 사용자 프롬프트의 4-5 항목이 우려한 "수직·수평 분할도 반드시 중복을 만든다는 오해"는, 저장소 자신도 "중복 허용"이라는 상위 범주로 4기법을 함께 묶어 설명하고 "데이터 자체의 복제 여부"를 별도 축으로 구분하지 않으므로, 저장소 표현 수준을 넘어서는 재구성은 하지 않고 위 표의 무결성 위험 예외 문장만 보강했다.

## 4. 누락되어 문제로 추가한 개념

| 개념 | 추가 문항 | 본문에 넣지 않은 이유 |
| --- | --- | --- |
| 엔터티·속성 명명규칙(단수 명사, 임의 약어 금지, 속성당 단일 의미, 이름 중복 방지) | 문항 8(신설) | `ENT-01`의 부제이자 반복 함정으로 다뤄지는 세부 규칙 목록이라, 본문 서사에 넣으면 "협업 합의"라는 서사 흐름이 끊기고 규칙 나열이 됨. 28문항 중 어디에도 없어 신설 |
| 일반속성(PK·FK 포함 여부에 따른 엔터티 구성방식 분류) vs 발생방식(기본·설계·파생) 분류 혼동 | 문항 10(신설) | `ATTR-02`가 반복 오답으로 명시한 분류축 혼동 함정. 기존 문항9(속성 발생방식)는 기본·설계·파생만 다루고 이 함정은 다루지 않음 |
| ERD 표기법(IE/Crow's Foot) 기호 판독 — 까치발·원·막대, IE는 NULL 미표기 | 문항 13(신설, [3점], 기호-의미 표(table) 포함) | `ERD-01`이 별도 장(제4장)으로 다루는 핵심 판정 기준이나 28문항에는 전혀 없었음. 이미지 삽입 대신 표 형태로 기호·의미를 제시(요청사항 6번 반영) |
| 식별자 구성방식 중 내부/외부·단일/복합 축(대표성 축은 기존 문항17에서 이미 다룸) | 문항 18(신설) | `ID-04`의 세 독립 축 중 기존 문항(현 17번)이 "주식별자/보조식별자/외부식별자"만 다뤄 "내부식별자" 명칭과 "단일/복합" 축이 미검증 상태였음 |
| ACID 성질별 적용 문제 | 추가 없음(기존 문항28 유지) | 기존 문항28이 이미 4가지 성질을 각각 다른 상황 서술에 대응시키는 적용형 문제였고, "이름만 맞히는" 단순 암기형이 아니었음 — 중복 추가하지 않음 |
| NULL 계산(NOT IN+NULL / SUM(A+B)-SUM(A)+SUM(B) / GROUP BY의 NULL 그룹 / Oracle 빈 문자열=NULL) | 추가 없음 | `NULL-집중학습/00~14` 14개 모듈(`08-NOT-IN과NOT-EXISTS`, `03-NULL과산술연산`, `04-NULL과집계함수`, `07-NULL과JOIN`, `09-NULL정렬과윈도우함수`, `12-평균-중앙값대치` 등)이 이미 quiz.sql·answer.sql·expected_results.md까지 갖춰 상세히 다루고 있어, 통합 문서에 복제하지 않고 해당 폴더를 참조하도록 안내함(6장 참고) |

새 문항 4개 삽입에 따라 **문항 8·10·13·18을 새로 배치하고, 기존 문항 8~28을 문항 9~32로 순차 재번호**했다. 해설 번호, 문항 번호, 표현 순서를 모두 일치시켰으며 4부 구조(협업의 서사 4개 부 / 제1~3편 순서 / 마지막 점검표)는 변경하지 않았다. 총 문항 수는 28 → 32.

## 5. 검증했으나 수정하지 않은 항목

| 개념 | 유지한 이유 | 근거 |
| --- | --- | --- |
| 4-1. 중복·비유연성·비일관성 정의 | DOCX 문장("애플리케이션을 조금만 바꿔도 모델까지 함께 흔들리는 비유연성", "연관관계를 명확히 정의하지 않아 일부 데이터만 바뀌는 비일관성")이 저장소 원문과 사실상 동일 표현 | `01-데이터모델링-총론/MOD-01-데이터모델링의-유의사항.md` 핵심 법리: "비유연성=애플리케이션과 데이터 모델의 연계성이 높아 사소한 변경에도 모델이 수시로 바뀌어야 하는 현상", "비일관성=데이터 간 연관 관계를 명확히 정의하지 않아 일부 데이터만 변경되어 정합성이 깨지는 현상" |
| 4-2. 엔터티 성립조건("인스턴스 2건 이상"의 의미, 속성 개수, 식별자 포함 여부, 예외 표현) | DOCX가 이미 정확한 4요건(인스턴스 2개 이상 **가능한 집합**, 속성 1개 이상, 관계 1개 이상 원칙+통계성·코드성 예외)을 저장소와 동일하게 서술. "인스턴스 2개 이상"은 "현재 2건이 저장돼 있어야 한다"가 아니라 "2개 이상의 인스턴스가 존재할 수 있는 집합"이라는 뜻이며, 식별자(유일성 등)는 성립요건에 포함되지 않는다는 것도 저장소와 일치 | `02-엔터티/ENT-01-엔터티-성립요건과-속성구분.md` 핵심 법리: "2개 이상의 인스턴스 집합이어야 하고 ① … ② 1개 이상의 속성 … ③ 관계 최소 1개"; `00-SQLD-1과목-고난도문제.md` L925: "통계성·코드성 엔터티는 관계 요건의 예외이지, 인스턴스·속성 요건의 예외는 아니다" |
| 4-3. 관계는 동사에서 도출("단서일 뿐 확정 아님"이라는 완화 필요 여부) | DOCX 문장이 저장소 원문과 동일. 저장소는 "동사가 근거"라고 단정적으로 서술하며 별도의 "단서일 뿐 확정은 아니다"라는 완화 조항을 두지 않음 — 오히려 완화하면 SQLD 정답 판정 기준("근거는 명사가 아니라 동사")이 흐려짐 | `04-관계와-ERD/REL-01-관계의-정의와-필수선택참여.md` 핵심 법리: "업무기술서에서 관계를 도출하는 근거는 명사가 아니라 동사(예: '가입하다', '주문하다')다" |
| ACID(본문에 길게 넣지 않고 문제로 유지) | 기존 문항28(현 문항32)이 이미 4성질 각각을 서로 다른 업무 상황 서술에 대응시키는 적용형 문제 | 문항32 지문: "주문 저장과 결제 저장 중 하나가 실패하면…／동시에 실행되는 다른 주문은…／정상 종료 후에는…／완료된 결과는 장애 후에도…" 4문장이 각각 원자성·고립성·일관성·지속성에 대응 |
| NULL 계산 세부(NOT IN, SUM 분배, GROUP BY, Oracle 빈문자열) | 통합 문서에 중복 수록하지 않음(6장 참고 안내로 대체) | `NULL-집중학습/` 14개 모듈이 이미 상세 커버 |

## 6. 수정 파일 목록

- `SQLD_1과목_협업형_본문_5줄지문_문제_수정본 (1).docx` → **수정 목적**: (1) 1부 서사 문단에서 모델링 3단계 순서 서술의 절대성 완화(4-4), (2) 해설26(구 해설22)에 반정규화 4기법 중 무결성 위험이 없는 예외(중복 관계 추가) 명시(4-5), (3) 명명규칙·PK/FK 구성방식·ERD 표기법·식별자 내부외부·단일복합 4개 문항 신설 및 문항 8~28 → 9~32 재번호(해설 포함)
- `SQLD_1과목_개념_검증_및_수정_보고서.md`(본 파일, 신규 생성) → **수정 목적**: 저장소 대조 검증 과정과 결론 기록

저장소의 `SQLD-1과목-핵심법리판례집/`, `Concepts/`, `SQLD-시험직전-압축/` 내 `.md` 파일은 **이번 검토에서 수정하지 않았다**(2장 참고 — Type A 오류가 발견되지 않았음).
