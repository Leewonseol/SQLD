-- 04. NULL과 집계함수 — 연습문제
-- null_lab_customer: score 값 85,92,NULL,77,60,0,88,95,NULL,70,82,99
-- (customer_id 3, 9가 NULL, customer_id=6은 실제 숫자 0)

-- =====================================================================
-- Q1. (객관식) null_lab_customer에서 COUNT(*)와 COUNT(score)를 각각
-- 실행했을 때 두 결과값은?
--
--   ① 둘 다 12
--   ② 둘 다 10
--   ③ COUNT(*)=12, COUNT(score)=10
--   ④ COUNT(*)=10, COUNT(score)=12
--
-- 답: (      )
-- =====================================================================


-- =====================================================================
-- Q2. (객관식) AVG(score)와 AVG(COALESCE(score, 0))에 대한 설명으로
-- 옳은 것은?
--
--   ① 두 값은 항상 같다(NULL은 계산에 영향을 주지 않으므로).
--   ② AVG(score)가 AVG(COALESCE(score,0))보다 크거나 같다(NULL을 무시한
--      평균이, NULL을 0으로 채운 평균보다 분모가 작기 때문).
--   ③ AVG(COALESCE(score,0))가 항상 더 크다.
--   ④ 어느 쪽이 더 큰지는 데이터에 따라 항상 예측 불가능하다.
--
-- 답: (      )
-- =====================================================================


-- =====================================================================
-- Q3. (SQL 작성) SQL Server에서 score(INT 컬럼)의 평균을 소수점 둘째
-- 자리까지 정확하게 구하는 쿼리를 작성하시오. (AVG(score)를 그대로 쓰면
-- 안 되는 이유를 함께 생각할 것)
--
-- 여기에 작성:



-- =====================================================================
-- Q4. (SQL 작성) null_lab_customer에서 score가 모두 NULL인 부서
-- (dept_code)만 골라 SUM(score)이 NULL로 나오는 것을 확인하는 쿼리를
-- 작성하시오. (WHERE score IS NULL로 필터링 후 GROUP BY 사용)
--
-- 여기에 작성:


