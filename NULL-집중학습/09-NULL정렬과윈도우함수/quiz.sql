-- 09. NULL 정렬과 윈도우 함수 — 연습문제
-- null_lab_customer.score: customer_id 3, 9만 NULL(나머지는 값 존재)
-- null_lab_customer.purchase_amt: customer_id 5, 11만 NULL
-- null_lab_customer.dept_code: customer_id 4, 10만 NULL

-- =====================================================================
-- Q1. (객관식) Oracle에서 `SELECT * FROM null_lab_customer ORDER BY score ASC;`
-- 를 실행했을 때 score가 NULL인 행(customer_id 3, 9)은 결과에서 어디에
-- 위치하는가?
--
--   ① 맨 앞
--   ② 맨 뒤
--   ③ score 값이 비슷한 행들 사이에 무작위로 섞인다
--   ④ 에러가 발생해 결과가 없다
--
-- 답: (      )
-- =====================================================================


-- =====================================================================
-- Q2. (객관식) 같은 쿼리를 SQL Server에서 실행하면 NULL 행의 위치는
-- Oracle과 비교했을 때 어떻게 다른가?
--
--   ① 완전히 같다(NULL 위치는 DBMS 무관하게 표준으로 고정되어 있다)
--   ② Oracle과 정반대다 — SQL Server는 NULL을 가장 작은 값으로 취급해
--      ASC일 때 맨 앞에 온다
--   ③ SQL Server는 NULL이 있는 컬럼으로 정렬 자체가 불가능하다
--   ④ SQL Server는 NULL 행을 자동으로 제외하고 정렬한다
--
-- 답: (      )
-- =====================================================================


-- =====================================================================
-- Q3. (SQL 작성) SQL Server에서 `NULLS LAST` 절 없이, score가 NULL인
-- 행이 오름차순 정렬 결과의 맨 뒤에 오도록(Oracle의 기본 ASC 동작과
-- 같은 결과가 나오도록) ORDER BY 절을 작성하시오.
--
-- 여기에 작성:



-- =====================================================================
-- Q4. (객관식) purchase_amt로 RANK()를 계산할 때, purchase_amt가 NULL인
-- 행이 2개 있다면 이 두 행의 RANK 값은 어떻게 되는가?
--
--   ① 서로 다른 값(NULL은 항상 서로 다르다고 취급되므로)
--   ② 서로 같은 값(정렬/순위 맥락에서는 NULL을 서로 같다고 취급하므로)
--   ③ RANK 계산이 아예 불가능해 에러가 발생한다
--   ④ 둘 다 NULL로 표시된다
--
-- 답: (      )
-- =====================================================================


-- =====================================================================
-- Q5. (SQL 작성) null_lab_customer를 dept_code로 파티션을 나누고, 각
-- 파티션 안에서 customer_id 오름차순으로 ROW_NUMBER를 매기시오.
-- dept_code가 NULL인 행들이 하나의 파티션으로 묶이는지 결과로 확인하시오.
--
-- 여기에 작성:


