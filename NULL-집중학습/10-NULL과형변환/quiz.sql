-- 10. NULL과 형변환 — 연습문제
-- null_lab_customer.membership_code: customer_id=5만 '0', 나머지는
-- 'A001'~'A012'(숫자로 변환 불가능)
-- null_lab_customer.join_date: customer_id 3, 11만 NULL

-- =====================================================================
-- Q1. (객관식) Oracle에서 `SELECT CAST(NULL AS NUMBER) FROM dual;`을
-- 실행한 결과는?
--
--   ① 0
--   ② NULL
--   ③ 에러 발생
--   ④ 빈 문자열
--
-- 답: (      )
-- =====================================================================


-- =====================================================================
-- Q2. (객관식) Oracle에서 다음 쿼리를 실행하면 어떤 일이 벌어지는가?
--
--   SELECT CAST(membership_code AS NUMBER) FROM null_lab_customer
--   WHERE customer_id = 1;   -- membership_code = 'A001'
--
--   ① NULL이 반환된다
--   ② 0이 반환된다
--   ③ ORA-01722(invalid number) 에러가 발생한다
--   ④ 'A001'이 그대로 반환된다
--
-- 답: (      )
-- =====================================================================


-- =====================================================================
-- Q3. (SQL 작성) SQL Server에서 null_lab_customer의 모든 행에 대해
-- membership_code를 정수로 안전하게 변환하되, 변환할 수 없는 값은
-- 에러 대신 NULL이 되도록 TRY_CAST를 사용해 조회하시오.
-- (customer_id, membership_code, 변환결과 컬럼을 조회)
--
-- 여기에 작성:



-- =====================================================================
-- Q4. (객관식) Oracle에는 SQL Server의 TRY_CAST/TRY_CONVERT에 직접
-- 대응하는 함수가 없다. Oracle 12c 이상에서 변환 실패 시 에러 대신
-- NULL(또는 지정한 기본값)을 받으려면 어떤 구문을 쓰는가?
--
--   ① CAST(... SAFE)
--   ② TO_NUMBER(... DEFAULT NULL ON CONVERSION ERROR)
--   ③ CONVERT(... IGNORE ERROR)
--   ④ NVL(CAST(...), NULL)
--
-- 답: (      )
-- =====================================================================


-- =====================================================================
-- Q5. (SQL 작성) Oracle에서 join_date를 'YYYY-MM-DD' 형식의 문자열로
-- 변환하여 조회하시오. join_date가 NULL인 행(customer_id 3, 11)의
-- 결과값이 무엇이 되는지도 주석으로 답하시오.
--
-- 여기에 작성:


-- 결과값(주석으로 서술):
