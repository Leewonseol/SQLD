-- 11. NULL과 제약조건 — 연습문제
-- null_lab_check_demo(demo_id PK, score CHECK(score>=0))
-- null_lab_unique_demo(demo_id PK, email UNIQUE)
-- null_lab_fk_demo(demo_id PK, dept_code FK -> null_lab_dept)

-- =====================================================================
-- Q1. (객관식) CHECK (score >= 0) 제약조건이 걸린 컬럼에 score=NULL인
-- 행을 INSERT하면 어떻게 되는가?
--
--   ① CHECK 위반으로 INSERT가 거부된다
--   ② NULL >= 0이 UNKNOWN으로 평가되고, CHECK는 FALSE가 아니면 통과
--      시키므로 INSERT가 성공한다
--   ③ NULL이 자동으로 0으로 바뀌어 INSERT된다
--   ④ 문법 오류가 발생한다
--
-- 답: (      )
-- =====================================================================


-- =====================================================================
-- Q2. (객관식) email 컬럼에 UNIQUE 제약조건만 걸려 있는 테이블에 email=
-- NULL인 행을 이미 하나 넣어둔 상태에서, email=NULL인 행을 한 번 더
-- INSERT하려 한다. Oracle과 SQL Server의 결과를 각각 바르게 짝지은
-- 것은?
--
--   ① Oracle 성공 / SQL Server 성공
--   ② Oracle 실패 / SQL Server 실패
--   ③ Oracle 성공 / SQL Server 실패
--   ④ Oracle 실패 / SQL Server 성공
--
-- 답: (      )
-- =====================================================================


-- =====================================================================
-- Q3. (객관식) PRIMARY KEY 컬럼에 NULL을 INSERT하려고 시도하면?
--
--   ① Oracle만 에러가 나고 SQL Server는 성공한다
--   ② SQL Server만 에러가 나고 Oracle은 성공한다
--   ③ 두 DBMS 모두 에러가 난다(PK는 NULL을 절대 허용하지 않음)
--   ④ 두 DBMS 모두 성공한다(PK도 UNIQUE처럼 NULL을 허용함)
--
-- 답: (      )
-- =====================================================================


-- =====================================================================
-- Q4. (SQL 작성) null_lab_fk_demo에 dept_code가 NULL인 행(demo_id=99)을
-- 추가하는 INSERT문을 작성하고, 이 INSERT가 성공하는지 실패하는지와
-- 그 이유를 주석으로 서술하시오.
--
-- 여기에 작성:


-- 성공/실패 및 이유(주석):
-- =====================================================================


-- =====================================================================
-- Q5. (서술) 어떤 개발자가 "score 컬럼에 NULL이 들어오면 안 된다"는
-- 규칙을 지키려고 CHECK (score >= 0) 제약조건 하나만 걸어두었다. 이
-- 제약조건이 실제로는 그 규칙을 지켜주지 못하는 이유를 설명하고, NULL을
-- 확실히 막으려면 어떤 제약조건을 추가로 걸어야 하는지 쓰시오.
--
-- 답:
