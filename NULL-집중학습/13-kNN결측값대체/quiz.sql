-- 13. kNN 결측값 대체 — 연습문제
-- impute_practice_raw: 도너 풀 15행, 대체 대상 5행(C016~C020).
-- k = round(sqrt(15)) = 4. C018은 4번째 근접이웃 자리에서 동률(tie) 발생.

-- =====================================================================
-- Q1. (객관식) 도너 풀이 15행일 때, Jonsson & Wohlin(2004) 규칙에 따른
-- k값은?
--
--   ① 3  ② 4  ③ 5  ④ 15
--
-- 답: (      )
-- =====================================================================


-- =====================================================================
-- Q2. (객관식) C017(recency, delivery_days가 모두 NULL)의 거리 계산에
-- 대한 설명으로 옳은 것은?
--
--   ① recency, delivery_days도 0으로 채워서 5개 변수 모두 사용한다
--   ② recency, delivery_days를 제외한 3개 변수만 사용하고, 3으로 정규화한다
--   ③ 거리 계산 자체가 불가능해 C017은 kNN 대체 대상에서 제외된다
--   ④ NULL이 있으면 무조건 거리가 0으로 계산된다
--
-- 답: (      )
-- =====================================================================


-- =====================================================================
-- Q3. (SQL 작성) knn_vs_baseline_result에서 kNN 대체(strict)의 오차 절댓값이
-- 평균 대체의 오차 절댓값보다 작은 행만 조회하시오.
--
-- 여기에 작성:



-- =====================================================================
-- Q4. (서술형) C019(near_extreme)에서 kNN(strict) 대체값(23876.25)이
-- 정답(94000)과 크게 차이 나는 이유를 2~3문장으로 설명하시오. (힌트:
-- 진짜 가까운 이웃은 C012 하나뿐이지만 k가 고정되어 있다)
--
-- 여기에 작성:


