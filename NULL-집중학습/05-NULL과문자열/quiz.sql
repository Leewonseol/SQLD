-- 05. NULL과 문자열 함수 — 연습문제
-- null_lab_customer: customer_id 2,8=nickname NULL / 3=nickname ''(Oracle에선 NULL,
-- SQL Server에선 '') / 4=nickname ' '(공백 한 칸) / 나머지=일반 문자열

-- =====================================================================
-- Q1. (객관식) SQL Server에서 다음 두 식의 결과가 다르게 나오는 행은
-- nickname 컬럼이 어떤 상태일 때인가?
--   식 A: customer_name + nickname
--   식 B: CONCAT(customer_name, nickname)
--
--   ① nickname이 일반 문자열일 때
--   ② nickname이 NULL일 때
--   ③ nickname이 ''(빈 문자열)일 때
--   ④ 두 식은 항상 결과가 같다
--
-- 답: (      )
-- =====================================================================


-- =====================================================================
-- Q2. (객관식) Oracle에서 다음 SQL을 실행하면?
--
--   SELECT CONCAT(customer_name, '-', nickname) FROM null_lab_customer;
--
--   ① 정상 실행되고 세 값이 이어붙는다
--   ② nickname이 NULL인 행만 에러가 난다
--   ③ 인수 개수 오류(ORA-00909)로 컴파일 자체가 실패한다
--   ④ nickname 부분이 무시되고 두 값만 이어붙는다
--
-- 답: (      )
-- =====================================================================


-- =====================================================================
-- Q3. (객관식) SQL Server에서 nickname 컬럼에 공백 한 칸(' ')짜리 값이
-- 들어있는 행의 LEN(nickname)은?
--
--   ① 1
--   ② 0
--   ③ NULL
--   ④ 에러
--
-- 답: (      )
-- =====================================================================


-- =====================================================================
-- Q4. (SQL 작성) SQL Server에서 nickname이 NULL이어도 결과 문자열 전체가
-- NULL이 되지 않도록 customer_name과 nickname을 "(닉네임)" 형태로 이어
-- 붙이는 SQL을 CONCAT 함수를 이용해 작성하시오. (별칭: full_label)
--
-- 여기에 작성:



-- =====================================================================
-- Q5. (SQL 작성) SQL Server에서 nickname 컬럼의 실제 저장 바이트 길이를
-- 정확히 구분해서(공백/빈 문자열도 서로 다르게) 조회하는 SQL을 작성하시오.
-- LEN이 아니라 정확한 함수를 사용해야 한다.
--
-- 여기에 작성:


