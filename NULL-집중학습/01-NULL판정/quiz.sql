-- 01. NULL 판정 — 연습문제
-- null_lab_customer: customer_id 2,8=nickname NULL / 3=nickname ''(빈문자열)
-- / 4=nickname ' '(공백 한 칸) / 나머지=일반 문자열

-- =====================================================================
-- Q1. (객관식) Oracle에서 VARCHAR2 컬럼에 빈 문자열 ''을 INSERT한 뒤
-- 그 컬럼을 IS NULL로 조회하면 결과는?
--
--   ① 항상 FALSE(빈 문자열은 NULL이 아니므로)
--   ② 항상 TRUE(Oracle이 저장 시 NULL로 바꾸므로)
--   ③ UNKNOWN
--   ④ 에러 발생
--
-- 답: (      )
-- =====================================================================


-- =====================================================================
-- Q2. (객관식) SQL Server에서 nickname 컬럼 값이 ''(빈 문자열)인 행을
-- `WHERE nickname = ''`으로 조회했다. 이 컬럼에 공백 한 칸(' ')짜리 값을
-- 가진 행이 있다면 이 쿼리의 결과는?
--
--   ① ''인 행만 나온다(공백 행은 절대 안 나온다)
--   ② ANSI 공백 패딩 비교 때문에 ''인 행과 ' '인 행이 모두 나온다
--   ③ 에러가 발생한다
--   ④ 아무 행도 나오지 않는다
--
-- 답: (      )
-- =====================================================================


-- =====================================================================
-- Q3. (SQL 작성) SQL Server에서 nickname이 정확히 빈 문자열(''이고 공백이
-- 아닌)인 행만 골라내는 조건을 작성하시오. (DATALENGTH 사용)
--
-- 여기에 작성:



-- =====================================================================
-- Q4. (SQL 작성) null_lab_customer에서 nickname 컬럼이 "값을 알 수 없는"
-- 상태인 행의 customer_id를 IS NULL을 이용해 조회하시오.
--
-- 여기에 작성:


